#pragma once

#include "pvp.h"

#define __attrconst

/*
 * Everything is BE already.
 */

#define fdt32_to_cpu(x) (x)
#define cpu_to_fdt32(x) (x)

#define fdt64_to_cpu(x) (x)
#define cpu_to_fdt64(x) (x)


